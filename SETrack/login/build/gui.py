# -*- coding: utf-8 -*-
# This file was generated by Tkinter Designer by Parth Jadhav
# https://github.com/ParthJadhav/Tkinter-Designer


from pathlib import Path
import ctypes
import sys

# from tkinter import *
# Explicit imports to satisfy Flake8
from tkinter import (
    BooleanVar,
    Button,
    Canvas,
    Checkbutton,
    Entry,
    Frame,
    Label,
    Listbox,
    PhotoImage,
    Radiobutton,
    StringVar,
    Text,
    Tk,
    ttk,
)

try:
    from PIL import Image, ImageTk
except ImportError:
    Image = None
    ImageTk = None


OUTPUT_PATH = Path(__file__).parent
ASSETS_PATH = OUTPUT_PATH / Path(r"assets\frame0")
THEME = ""
IMAGE_REFS = []


def relative_to_assets(path: str) -> Path:
    return ASSETS_PATH / Path(path)


def set_assets_path(path: str):
    global ASSETS_PATH
    ASSETS_PATH = OUTPUT_PATH / Path(path)


def load_photo_image(path: str):
    try:
        image = PhotoImage(file=path)
    except Exception:
        if Image is None or ImageTk is None:
            raise
        image = ImageTk.PhotoImage(Image.open(path))
    IMAGE_REFS.append(image)
    return image


def enable_dpi_awareness():
    if sys.platform != "win32":
        return
    try:
        ctypes.windll.shcore.SetProcessDpiAwareness(1)
    except Exception:
        try:
            ctypes.windll.user32.SetProcessDPIAware()
        except Exception:
            pass


def center_window(window, width, height):
    window.update_idletasks()
    x = int((window.winfo_screenwidth() - width) / 2)
    y = int((window.winfo_screenheight() - height) / 2)
    window.geometry(f"{width}x{height}+{x}+{y}") #x, y colt stg sus


def create_rounded_rectangle(canvas, x1, y1, x2, y2, radius, **kwargs):
    radius = max(0, min(radius, abs(x2 - x1) / 2, abs(y2 - y1) / 2))
    if radius == 0:
        return canvas.create_rectangle(x1, y1, x2, y2, **kwargs)
    points = [
        x1 + radius, y1,
        x2 - radius, y1,
        x2, y1,
        x2, y1 + radius,
        x2, y2 - radius,
        x2, y2,
        x2 - radius, y2,
        x1 + radius, y2,
        x1, y2,
        x1, y2 - radius,
        x1, y1 + radius,
        x1, y1,
    ]
    return canvas.create_polygon(points, smooth=True, **kwargs)


class ImageButton(Label):
    def __init__(self, master=None, command=None, **kwargs):
        super().__init__(master, **kwargs)
        self._command = command
        self.configure(cursor="hand2")
        self.bind("<Button-1>", self._invoke)

    def _invoke(self, event):
        if self._command is not None:
            self._command()

    def configure(self, cnf=None, **kwargs):
        if cnf and "command" in cnf:
            cnf = dict(cnf)
            self._command = cnf.pop("command")
        if "command" in kwargs:
            self._command = kwargs.pop("command")
        return super().configure(cnf, **kwargs)

    config = configure


def apply_theme(window):
    if not THEME:
        return
    try:
        ttk.Style(window).theme_use(THEME)
    except Exception:
        pass


enable_dpi_awareness()

window = Tk()
apply_theme(window)

window.geometry("400x200")
window.configure(bg="#D9D9D9")
center_window(window, 400, 200)


canvas = Canvas( #canva principal
    window,
    bg="#D9D9D9",
    height=200,
    width=400,
    bd=0,
    highlightthickness=0,
    relief="ridge"
)

canvas.place(x=0, y=0)
canvas.create_rectangle(
    16.0,
    112.0,
    386.0,
    128.0,
    fill="#C5C5C5",
    outline="")

canvas.create_rectangle(
    16.0,
    65.0,
    386.0,
    81.0,
    fill="#C5C5C5",
    outline="")

canvas.create_rectangle(
    -204.0,
    -183.0,
    112.0,
    -179.0,
    fill="#C5C5C5",
    outline="")

canvas.create_text(
    281.0,
    168.0,
    anchor="nw",
    text="™CodeSinaia 2026",
    fill="#000000",
    font=("Inter", 12 * -1, "normal", "roman")
)

canvas.create_text(
    16.0,
    168.0,
    anchor="nw",
    text="©2026 Inproted",
    fill="#000000",
    font=("Inter", 12 * -1, "normal", "roman")
)

canvas.create_text(
    157.0,
    4.0,
    anchor="nw",
    text="Login",
    fill="#000000",
    font=("Inter", 32 * -1, "normal", "roman")
)

canvas.create_text(
    17.0,
    48.0,
    anchor="nw",
    text="Username",
    fill="#000000",
    font=("Inter", 14 * -1, "normal", "roman")
)

canvas.create_text(
    17.0,
    95.0,
    anchor="nw",
    text="Password",
    fill="#000000",
    font=("Inter", 14 * -1, "normal", "roman")
)

button_image_1 = load_photo_image(
    relative_to_assets("button_1.png"))
button_1 = ImageButton(
    window,
    image=button_image_1,
    borderwidth=0,
    highlightthickness=0,
    command=lambda: print("button_1 clicked"), #lambda face un prepatch, asteapta sa apasam noi butonul
    relief="flat",
    background="#C5C5C5"
)
button_1.place(
    x=111.0,
    y=160.0,
    width=80.0,
    height=34.0
)

button_image_2 = load_photo_image(
    relative_to_assets("button_2.png"))
button_2 = ImageButton(
    window,
    image=button_image_2,
    borderwidth=0,
    highlightthickness=0,
    command=lambda: print("button_2 clicked"),
    relief="flat",
    background="#C5C5C5"
)
button_2.place(
    x=194.0,
    y=160.0,
    width=86.0,
    height=34.0
)
window.resizable(False, False)

if __name__ == "__main__": #verifica daca e primul fisier rulat sau modul importat
    window.mainloop()